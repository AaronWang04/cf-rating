{
  "name": "cf-rating",
  "version": "1.0.0",
  "private": true,
  "description": "A browser extension that shows Codeforces problem ratings without revealing tags.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/content.js && npm test"
  }
}
